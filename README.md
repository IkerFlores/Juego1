Para conocer mas a fondo sobre lo que es el programa de juego de piedra papel o tijera vamos a ir detallando lo mas posible las funcionalidades que tiene, como esta contemplado en los comentarios de el codigo a ejecutar, para esto empezamos con la libreria import random la cual nos otorga un dato aleatorio producido por la computadora, luego de esto vamos a enfocarnos dentro de la funcion que hay un if el cual nos ayudara a saber si existe un empate como primer proceso, luego de esto tenemos que validar que en las lineas esten las regls principales del juego de piedra papel o tijera tod esto lo vamos a colocar que el jugador sino caso contrario la computadora gano 
luego de esto en una funcion diferente vamos a hacer una funcion principal la cual se llama jugar la cual nos otorgara mayor detalle para el procesamiento completo de las funcionaldiades del programa, en una lista vamos a colocar los string piedra papel o tijera que son las opciones, luego de esto vamos a inicializar en 0 dos variables las cuales son las puntuaciones que tengan cada uno de los aprticipantes, en este caso dos participantes.
luego de esto vamos a proceder a recibir con un mensaje de bienvenida a el jugador, y dejar en claro que en este juego se define el ganador a el mejor de 3, luego mediante un while vamos a saber si es que se llega a dos victorias gana por como se define el mejor de 3, luego en la siguiente linea vamos a mostrar a cada uno de los participantes con su respectivo porcentaje, y mediante un input esperamos que elija una opcion, escribiendo de tal manera que sea una de las opciones, ponemos un .lower ya que mediante esto podemos definir que las palabras sean minusculas a toda costa, luego mediante un if dentro del mismo while vamos a colocar la opcion salir la cual si escribe la palabra se va a mostrar un mensaje que se ha rendido con el puntaje
luego en otro if vamos a verificar que las opciones ingresadas sean las que teniamos dentro de la lista, sino arrojamos un mensaje que diga error,luego en una variable de opcion para computadora vamos a tener que llamar a la libreria que se menciono al principio para que arroje un dato aleatorio luego de eso leemos la opcion que eligio la computadora luego en una variable llamada resultado vamos a llamar a la funcion primera que era para elegir un ganador con las dos opciones de los jugadores elegidas anteriormente, luego si es que arroja los menasjes que se eligieron al principio de esta funcion vamos a sumar un punto para cada caso que corresponda de jugadores con su respectivo mensaje, despues cuando el marcador sea igual no se sumaran puntos
finalmente vamos a presentar un texto el cual diga quien gano el juego mediante un if que sea igual a 2 el correspondiente ganador, por ultimo llamamos a la funcion principal jugar para poder ejecutar de forma correcta el programa.
